# Where would you fit your automated tests in your Recipe project development pipeline? Select one of the following and explain why
- Option 1. This ensures tests run automatically for every change, removing the risk of a developer forgetting to run them manually or catching bugs too late. For a recipe project, this is especially valuable because it protects shared features like ingredient scaling or search from silent regressions as the codebase grows.

# Would you use an end to end test to check if a function is returning the correct output? (yes/no)
- No.

# What is the difference between navigation and snapshot mode?
- Navigation mode tests how a webpage loads and performs over time, like measuring speed and responsiveness during the loading process. Snapshot mode captures the page as it currently is and checks things like accessibility, without caring about how it got there.

# Name three things we could do to improve the CSE 110 shop site based on the Lighthouse results.
- Three improvements for the CSE 110 shop would be: caching files longer so returning visitors load the page faster, reducing resources that block the page from displaying quickly, and adding a language attribute to the HTML so screen readers and browsers know what language the page is in.


# Screenshot
![screenshot test](lab7Screenshot.png)
